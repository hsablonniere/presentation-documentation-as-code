@import './fonts.styl'
@import './base-slide-responsive.styl'
@import './base-notes.styl'

.bespoke-slide
  font-family 'Futura PT'

@import './tpl-poster.styl'
@import './tpl-media.styl'
@import './tpl-quote.styl'
@import './tpl-striped-titles.styl'
@import './tpl-title.styl'
@import './tpl-code.styl'
@import './tpl-text.styl'
@import './tpl-code-stuff.styl'

@import '../node_modules/prism-themes/themes/prism-ghcolors.css'

section
  font-size 4rem

.bespoke-slide > p > code
  font-family OperatorMono
  font-weight bold

#asciidoc code strong
  color #2179c5
  font-weight normal

code[class*="language-"]
pre[class*="language-"]
  border none
  color #222
  font-family OperatorMono
  font-size 1rem
  font-weight 300
  line-height calc(1 / 0.6)
  margin 0
  padding 0
  // .token.string
  // font-style italic

.cue
  display none !important

figure
blockquote
p
  margin 0

.tpl-blank
  background-color #000

.tpl-blank--polaroid
  background-color #ff9

#conversion-2
#conversion-3
#ast
#ast-2
  .image
    transition 150ms transform ease-in-out

.toggle-state
  #ast
  #ast-2
    .image
      transform scale(2.5) translate3d(0, -12%, 0)

.toggle-state
  #conversion-2
  #conversion-3
    .image
      transform scale(2) translate3d(0, -40%, 0)

#title
  .tpl-poster-header
  .tpl-poster-footer
    transition 150ms transform ease-in-out

.toggle-state
  #title .tpl-poster-header
    transform translate3d(-20%, -110%, 0)
  #title .tpl-poster-footer
    transform translate3d(20%, 110%, 0)

@media (max-height 450px) {
  * {
    animation none !important
    transition-duration 0s !important
  }
}
